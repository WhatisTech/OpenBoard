# Node modules
/node_modules
/oh_modules

# Build outputs
**/build
**/.preview
/.hvigor
.cxx

# IDE and editor files
/.idea
/.vscode
.DS_Store

# DevEco Studio
/local.properties
*.iml
.gradle
.externalNativeBuild

# C/C++ tooling
/.clangd
/.clang-format
/.clang-tidy

# Test directories
**/.test
/.appanalyzer

# Lock files
# Recommended: COMMIT lock files for reproducible builds
# Only ignore if working solo or prefer flexible dependency versions
# Uncomment the following line to ignore lock files:
# oh-package-lock.json5
# **/oh-package-lock.json5

# Signing certificates and keys (NEVER commit these!)
*.p12
*.cer
*.p7b
*.jks
*.keystore

# Personal signing config (generated by DevEco Studio)
# Developers should generate their own signing configs
# Uncomment the following line if your build-profile contains signing material:
# **/build-profile.json5

# Claude Code assistant folder
.claude

# Temporary files
*.tmp
*.log
*.swp
*.bak
*~